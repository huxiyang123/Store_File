package homework2;
/*
 * 2.构造两个接口，A和B，分别包含有两个不同的函数，
 * 使用B接口继承A接口。创建一个具体类，实现B接口，
 * 在类中只实现B接口中所定义的方法，
 * 尝试编译，并观察编译信息。
 */
public interface A {
	public abstract void say();
	public abstract void doSome();
}
